import React from 'react';
import Navigation from '../components/Navigation';

const Surveys = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Surveys;