import React from 'react';
import Navigation from '../components/Navigation';

const Laboratories = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Laboratories;