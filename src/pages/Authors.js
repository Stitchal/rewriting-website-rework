import React from 'react';
import Navigation from '../components/Navigation';

const Authors = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Authors;