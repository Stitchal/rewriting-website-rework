import React from 'react';
import Navigation from '../components/Navigation';

const Courses = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Courses;