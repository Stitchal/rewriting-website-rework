import React from 'react';
import Navigation from '../components/Navigation';

const Venues = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Venues;